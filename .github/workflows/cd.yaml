name: CD

on:
  workflow_dispatch:
  push:
    tags:
      - '*.*.*'
      - '!v*'

jobs:
  gen:
    uses: katastroma/skeuotheke/.github/workflows/release-gen-proto.yaml@v0
    permissions:
      contents: write
