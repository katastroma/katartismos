//revive:disable:package-comments
package katartismos

const (
	// TenantLabel is the label where the tenant information is kept
	TenantLabel = "katastroma.org/tenant"
	// RepoLabel is the label where the repo information is kept
	RepoLabel = "katastroma.org/repo"
	// RefLabel is the label where the ref information is kept
	RefLabel = "katastroma.org/ref"
	// PathLabel is the label where the path information is kept
	PathLabel = "katastroma.org/path"
)
