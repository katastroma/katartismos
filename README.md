# Katartismos

Provisioner interface for [katastroma](https://github.com/katastroma). Defines
the client-facing API for provisioner implementations.

A provisioner receives manifests and applies them to the cluster using
server-side apply.
